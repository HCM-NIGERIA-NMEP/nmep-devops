apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: nmep-central-prd-appset-business
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: egov-hrms
          - name: dashboard-analytics

  template:
    metadata:
      name: 'egov-{{name}}'

    spec:
      project: egov-project

      source:
        repoURL: git@github.com:HCM-NIGERIA-NMEP/nmep-devops.git
        targetRevision: nmep-central-prd
        path: config-as-code/helm/charts/business-services/{{name}}
        helm:
          valueFiles:
            - values.yaml
            - ../../../../environments/ng-nmep-central-prd.yaml

      destination:
        server: https://kubernetes.default.svc
        namespace: egov

      syncPolicy:
        automated:
          prune: false
          selfHeal: true
        syncOptions:
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
        retry:
          limit: 10
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m0s
      ignoreDifferences:
        - group: apps
          kind: Deployment
          jsonPointers:
            - /spec/replicas