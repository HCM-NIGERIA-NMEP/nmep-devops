apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: central-uat-dev-appset-frontend
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: digit-ui
          - name: workbench-ui
          - name: dashboard-ui
          - name: microplan-ui
          - name: payments-ui

  template:
    metadata:
      name: 'dev-{{name}}'

    spec:
      project: dev-project

      source:
        repoURL: git@github.com:HCM-KEBBI/kebbi-devops.git
        targetRevision: central-uat-build
        path: config-as-code/helm/charts/frontend/{{name}}
        helm:
          valueFiles:
            - values.yaml
            - ../../../../environments/dev-central-uat.yaml

      destination:
        server: https://kubernetes.default.svc
        namespace: dev

      syncPolicy:
        automated:
          prune: false
          selfHeal: true
        syncOptions:
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
        retry:
          limit: 100
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m0s
